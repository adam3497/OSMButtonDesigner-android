<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorSecondaryDark</item>
        <item name="colorPrimaryDark">@drawable/status_bar_background</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.DefaultActivity">
        <item name="android:textColor">#000</item>
        <item name="android:windowActionBar">@color/colorSecondary</item>
    </style>

    <style name="AppTheme.NavBar">
        <item name="android:textColor">#fff</item>
    </style>

</resources>
